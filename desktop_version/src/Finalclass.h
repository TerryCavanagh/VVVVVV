#ifndef FINALCLASS_H
#define FINALCLASS_H

class finalclass
{
public:
    const short* loadlevel(int rx, int ry);

    const char* roomname;
    bool roomname_special;
    bool warpx, warpy;
};

#endif /* FINALCLASS_H */
